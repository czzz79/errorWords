# TODO

## Target-blind 中文音节文本预处理

**状态：暂不实现、不启动 TTS/ASR。**

目标：用只依赖 canonical 术语的通用算法生成中文音节扰动文本，评估时才读取 GT 混淆词。
不得根据当前 GT 目标决定声调、替换方向、插入位置或删除位置。

计划：

1. 建立固定拼音近邻图：声母、韵母和有限整音节近邻，按通用音系距离或公开模糊拼音规则排序。
2. 建立固定全局声调候选表：保留原声调，并按固定权重加入有限相邻声调/轻声候选。
3. 对每个音节位置统一枚举有限的删除、相邻音节重复和少量高频弱读插入；不从 GT 推断位置或插入音节。
4. 对候选拼音使用现有词典检索不同汉字的严格读音载体，排除 canonical 和已知 GT 混淆词文本。
5. 先对 `gt3.txt` 全量术语做 preprocess-only，设置每术语固定预算（建议 20～40 个候选），审计样本量和载体质量后再决定是否跑 TTS/ASR。
6. 报告中将此路线与 GT 定向诊断分开：主指标仅使用 target-blind 生成结果。

当前 `derived-components-delta-matrix` 和 `tone-edits-delta-matrix-r2` 是 GT 定向诊断结果，
不应作为此待办完成前的公平主指标。
